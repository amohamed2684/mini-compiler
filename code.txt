let x = 10;
let y = 20;
/* This is a
multi-line comment */
if (x < y) {
    print("x is less than y");
    /* Another multi-line comment
       that spans multiple lines */
    x = x + y;  // Single-line comment
}